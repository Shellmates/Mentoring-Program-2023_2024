services:
  hide-and-ride:
    build: .
    ports:
      - "1202:22"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      resources:
        limits:
          memory: 50M
