services:
  server:
    build: .
    ports:
      - '8000:8000'
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      resources:
        limits:
          memory: 50M

